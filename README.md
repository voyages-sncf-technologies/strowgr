
```shell
(cd webapp && make docker-build && make docker-image)
(cd haaasd && make docker-build && make docker-image)
```

### Build all

```shell
make
```

### Generate the docs

```shell
make docs
```
